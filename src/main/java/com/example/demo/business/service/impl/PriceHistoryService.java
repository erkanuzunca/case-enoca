package com.example.demo.business.service.impl;

import com.example.demo.data.entity.PriceHistoryEntity;
import com.example.demo.data.entity.ProductEntity;
import com.example.demo.data.repository.PriceHistoryRepository;
import org.springframework.stereotype.Service;

import java.time.LocalDateTime;

@Service
public class PriceHistoryService {

    // private final PriceHistoryRepository priceHistoryRepository;

    //public PriceHistoryService(PriceHistoryRepository priceHistoryRepository) {
    //    this.priceHistoryRepository = priceHistoryRepository;
   // }

  //  public void addPriceHistory(ProductEntity product, double price) {
    //    PriceHistoryEntity priceHistory = new PriceHistoryEntity();
    //    priceHistory.setProduct(product);
    //    priceHistory.setPrice(price);
    //    priceHistory.setCreatedAt(LocalDateTime.now());
    //    priceHistoryRepository.save(priceHistory);
   // }
}
